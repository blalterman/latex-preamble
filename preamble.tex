\usepackage[square,numbers, sort&compress]{natbib}
\usepackage{graphicx}
\usepackage[table]{xcolor}
\usepackage{hyperref}
\usepackage{xparse}
\usepackage{bm}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{latexsym}
\usepackage[capitalize]{cleveref}
%\usepackage[printonlyused]{acronym}

\usepackage[paper=letterpaper,left=1.00in,right=1.00in,top=1.0in,bottom=1.0in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[font=footnotesize]{caption}
\usepackage{subcaption}
\usepackage{wrapfig}
\usepackage{comment}
\usepackage{soul}
\usepackage{paralist}
\usepackage{tikz}
\usepackage{multicol}

%\NewDocumentCommand{\ProposalParagraph}{m o}{
%\addcontentsline{toc}{subsection}{#1}
%\paragraph{{\IfNoValueTF{#2}{#1}{#2}.}}
%}

%\newcommand{\appref}[1]{Appendix~\ref{#1}}
%\newcommand{\figref}[1]{Figure~\ref{#1}}
%\newcommand{\secref}[1]{\S\ref{#1}}
%%\newcommand{\eqref}[1]{equation~(\ref{#1})}
%\newcommand{\eqsref}[2]{equations~(\ref{#1})~and~(\ref{#2})}

\newcommand{\blankline}{\quad\pagebreak[2]}

\newcommand{\ppp}{{\tt Pegasus++} }

%% This gives us fun enumeration environments. compactenum will be nice.
\usepackage{paralist}

% An itemize-style list with little space between items
\newenvironment{innerlist}[1][\enskip\textbullet]%
        {\begin{compactenum}[#1]}{\end{compactenum}}

        \begin{document}

\setlength{\abovedisplayskip}{3pt}
\setlength{\belowdisplayskip}{3pt}
\captionsetup{aboveskip=-5pt}
\captionsetup{belowskip=-20pt}

\pagenumbering{roman}

%\input{latex-preamble/abbreviations}
\input{latex-preamble/vectors}
\input{latex-preamble/units}
\input{latex-preamble/quantities}
%\input{latex-preamble/spacecraft-instruments}
\input{latex-preamble/special-text}

\input{latex-preamble/annotation}

\newcommand{\Wind}{\emph{Wind}}